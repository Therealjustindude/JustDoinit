<%= form_with(model: doinit, local: true) do |form| %>
  <% if doinit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(doinit.errors.count, "error") %> prohibited this doinit from being saved:</h2>

      <ul>
        <% doinit.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :importance %>
    <%= form.number_field :importance %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
